# License Server + Web Dashboard

Run:
1. Node.js 20+
2. npm install
3. Set PORT, DB_PATH and a strong ADMIN_TOKEN.
4. npm start
5. Open http://localhost:3000

The dashboard lets you create, view, revoke and activate licenses.
For public deployment, use HTTPS and keep ADMIN_TOKEN secret.
